# Requirements for production environments should go in here.
# These will become dependencies of your pip package
# One per line, and version ranges can be specified (see PEP-440)
# For example:
# flask >= 1.1.1, < 2
aiohttp ~= 3.6.2
aiogoogle ~= 0.1.16
aiomysql >= 0.0.20, < 1
bonsai >= 1.2.0, < 2
google-auth >= 1.11.2, < 2
hiredis >= 1.0.1, < 2
slackclient >= 2.5.0, < 3
